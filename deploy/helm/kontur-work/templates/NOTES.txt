Контур Work установлен.

Проверка rollout:
  kubectl -n {{ .Release.Namespace }} rollout status deployment/{{ include "kontur-work.fullname" . }}-app
  kubectl -n {{ .Release.Namespace }} rollout status deployment/{{ include "kontur-work.fullname" . }}-worker
{{- if .Values.livekit.enabled }}
  kubectl -n {{ .Release.Namespace }} rollout status deployment/{{ include "kontur-work.fullname" . }}-livekit
{{- end }}

{{- if .Values.ingress.enabled }}
URL: {{ .Values.app.url }}
{{- else }}
Локальный доступ:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "kontur-work.fullname" . }} 3000:{{ .Values.service.port }}
{{- end }}

Состояние зависимостей:
  curl {{ .Values.app.url }}/api/health
{{- if .Values.livekit.enabled }}

LiveKit signal URL: {{ .Values.livekit.publicUrl }}
Media firewall: TCP/{{ .Values.livekit.rtc.tcpPort }}, UDP/{{ .Values.livekit.rtc.udpPort }}, UDP/{{ .Values.livekit.turn.udpPort }}{{ if .Values.livekit.turn.tls.enabled }}, TCP/{{ .Values.livekit.turn.tls.port }}{{ end }}
{{- end }}
