"use client";
import {useEffect} from 'react';
import {useWork,ErrorLine,WorkHeader} from './WorkUI.jsx';
const names={outbox:'События платформы',integrations:'Доставка интеграций',ai:'Генерации ИИ',semantic:'Индексация поиска',jira:'Импорт Jira',transcription:'Расшифровка записей'};
export default function OperationsView(){const api=useWork('operations');useEffect(()=>{const timer=setInterval(api.reload,30000);return()=>clearInterval(timer);},[api.reload]);return <section className="work-module"><WorkHeader title="Состояние платформы" text="Компоненты инфраструктуры и фоновые операции рабочего пространства."><button className="secondary" disabled={api.loading} onClick={api.reload}>Проверить</button></WorkHeader><ErrorLine error={api.error}/>{api.value&&<><p>Проверено: {new Date(api.value.checked_at).toLocaleString('ru-RU')}</p><div className="work-project-shifts">{Object.entries(api.value.dependencies).map(([name,d])=><article className="work-card" key={name}><h3>{name.toUpperCase()}</h3><span className={`work-status ${d.ok?'approved':'rejected'}`}>{d.ok?'Доступен':'Ошибка проверки'}</span><p>{d.latency_ms} мс</p></article>)}{api.value.workers.map(w=><article className="work-card" key={w.component}><h3>{w.component==='worker'?'Фоновые задачи':'Обработка записей'}</h3><p>Активных процессов: <strong>{w.active}</strong></p><small>Сигнал за последние 90 секунд</small></article>)}</div><div className="work-table-scroll"><table className="work-table"><thead><tr><th>Очередь</th><th>Ожидают / выполняются</th><th>Возраст старейшей операции</th></tr></thead><tbody>{api.value.queues.map(q=><tr key={q.name}><td>{names[q.name]}</td><td>{q.total}</td><td>{q.total?`${Math.max(0,Math.round(q.oldest_seconds||0))} с`:'—'}</td></tr>)}</tbody></table></div><p className="work-muted">{api.value.note}</p><p>Метрики для Prometheus: <code>/api/work/operations/metrics</code>. Используйте отдельный токен с областью operations:read и правом «Просмотр состояния платформы».</p></>}</section>;}
